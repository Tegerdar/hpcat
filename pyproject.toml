# pyproject.toml
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hpcat"
version = "0.1.0"
description = "HPC Administration & Monitoring"
requires-python = ">=3.9"

[project.scripts]
hpcat = "hpcat.cli:main"
